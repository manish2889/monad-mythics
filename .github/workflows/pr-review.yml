name: Automated PR Review and Analysis

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main, master, develop]

permissions:
  contents: read
  pull-requests: write
  checks: write
  statuses: write

jobs:
  # Security and vulnerability scanning
  security-scan:
    name: Security Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run security audit (fail on moderate+)
        run: npm audit --audit-level=moderate

      - name: CodeQL Init
        uses: github/codeql-action/init@v3
        with:
          languages: javascript,typescript
      - name: CodeQL Analyze
        uses: github/codeql-action/analyze@v3

  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Run ESLint
        run: npm run lint
      - name: Run Prettier check
        run: npm run format:check
      - name: Run tests with coverage
        run: npm run test:coverage
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  # Build and deployment verification
  build-verification:
    name: Build & Deployment Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
  run: npm ci

      - name: Verify Vercel deployment readiness
        run: npm run prepare-vercel

  # AI-powered code review
  ai-code-review:
    name: AI Code Review
    runs-on: ubuntu-latest
    needs: [security-scan, code-quality, build-verification]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Security Audit (high severity)
        run: npm audit --audit-level=high || true
        env:
          DEFAULT_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_ALL_CODEBASE: false
          VALIDATE_JAVASCRIPT_ES: true
          VALIDATE_TYPESCRIPT_ES: true

  # Performance and bundle analysis
  performance-analysis:
    name: Performance Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for analysis
        run: npm run build

      - name: Analyze bundle size
        run: |
          npx next-bundle-analyzer || echo "Bundle analysis completed"
          echo "Bundle size analysis completed - check build output for details"

  # Final PR status check
  pr-status:
    name: PR Status Summary
    runs-on: ubuntu-latest
    needs: [security-scan, code-quality, build-verification, ai-code-review, performance-analysis]
    if: always()
    steps:
      - name: Check all jobs status
        id: status-check
        continue-on-error: true
        run: |
          if [[ "${{ needs.security-scan.result }}" == "success" && 
                "${{ needs.code-quality.result }}" == "success" && 
                "${{ needs.build-verification.result }}" == "success" && 
                "${{ needs.ai-code-review.result }}" == "success" && 
                "${{ needs.performance-analysis.result }}" == "success" ]]; then
            echo "✅ All checks passed! PR is ready for review."
            echo "status=success" >> $GITHUB_OUTPUT
            echo "message=✅ All checks passed! PR is ready for review." >> $GITHUB_OUTPUT
          else
            echo "❌ Some checks failed. Please review and fix issues."
            echo "status=failure" >> $GITHUB_OUTPUT
            echo "message=❌ Some checks failed. Please review and fix issues." >> $GITHUB_OUTPUT
          fi

      - name: Comment PR status
        uses: actions/github-script@v7
        with:
          script: |
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            
            const botComment = comments.find(comment => 
              comment.user.type === 'Bot' && comment.body.includes('🤖 Automated PR Review Summary')
            );
            
            const status = '${{ needs.security-scan.result }}' === 'success' &&
                          '${{ needs.code-quality.result }}' === 'success' &&
                          '${{ needs.build-verification.result }}' === 'success' &&
                          '${{ needs.ai-code-review.result }}' === 'success' &&
                          '${{ needs.performance-analysis.result }}' === 'success';
            
            const body = `## 🤖 Automated PR Review Summary
            
            **Status:** ${status ? '✅ All checks passed' : '❌ Some checks failed'}
            
            ### Check Results:
            - **Security Scan:** ${{ needs.security-scan.result == 'success' && '✅' || '❌' }} ${{ needs.security-scan.result }}
            - **Code Quality:** ${{ needs.code-quality.result == 'success' && '✅' || '❌' }} ${{ needs.code-quality.result }}
            - **Build Verification:** ${{ needs.build-verification.result == 'success' && '✅' || '❌' }} ${{ needs.build-verification.result }}
            - **AI Code Review:** ${{ needs.ai-code-review.result == 'success' && '✅' || '❌' }} ${{ needs.ai-code-review.result }}
            - **Performance Analysis:** ${{ needs.performance-analysis.result == 'success' && '✅' || '❌' }} ${{ needs.performance-analysis.result }}
            
            ${status ? 
              '🎉 **This PR is ready for human review!**' : 
              '⚠️ **Please address the failing checks before requesting review.**'
            }
            
            ---
            *This comment is automatically updated on each push.*`;
            
            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

      - name: Fail job if checks failed
        if: steps.status-check.outputs.status == 'failure'
        run: |
          echo "${{ steps.status-check.outputs.message }}"
          exit 1
